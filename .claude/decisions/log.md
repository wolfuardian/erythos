# Decisions Log

> Long-term decisions for the Erythos project. One conclusion per line, **self-explanatory** (readable three months from now without context).
>
> Format: `- YYYY-MM-DD [tag]: <conclusion>`
>
> Rules:
> - Conclusions only — no process notes.
> - One line per decision; if it doesn't fit, split or it's not a decision.
> - Audit / review output without a clear decision: do not append.
>
> Maintenance: `/distill` or `/janitor` append automatically; manual append also OK.

---

- 2026-04 [solid-reactivity]: VectorDrag uses `<Index>` not `<For>` over primitive arrays — `<For>` is keyed by value, so `0.3 → 0.34` unmounts/remounts the child and wipes `onCleanup` listeners (issue #436)
- 2026-04 [solid-reactivity]: three repeated traps in derived UI — `createSignal(expr)` only samples once, `<Show>` children capture once on falsy→truthy switch (use `<Dynamic>` or `keyed`), and preview vs commit state must come from separate sources during interactive drag (issues #547→#553)
- 2026-05-04 [refactor]: splitting heat-map files reduces churn-score by ~half but doesn't relocate the heat — viewport stayed #1 even after `1061 → 643` lines, because it's churn-driven by feature work, not size-driven; future big-file decisions should weigh churn nature (feat-heavy vs fix-heavy) over raw line count
- 2026-05-05 [arch]: core/Editor↔Command type-only circular import is Command-pattern essential — both ends use `import type` (zero runtime cycle, just madge's design-layer warning); `editor.execute/undo/redo` are thin wrappers over `editor.history.*` for 51 callers' ergonomics; treated as accepted baseline, do not refactor unless async-command migration forces it
- 2026-05-05 [arch]: App.tsx churn (39 commits/30d) is feature-wiring noise, not lifecycle-coupling pain — no "change one thing, edit N places" signal; openProject/closeProject orchestration belongs to root component by nature; do not extract `useProjectLifecycle` hook (no second caller, idiom change without boundary fix, lifecycle-order migration risk asymmetric to gain)
- 2026-05-05 [types]: ID domains use branded types (`NodeUUID` / `PrefabId` / `AssetPath` / `BlobURL` in `src/utils/branded.ts`); mint at three boundaries — FS read (ProjectFile construction), JSON deserialise (`SceneDocument.deserialize`, `PrefabSerializer.deserializeFromPrefab`, `projectSession` localStorage), and external libs (`obj.uuid` from Three.js); caches that treat key as opaque (e.g. `PrefabRegistry._pathToURL` URL value, `ResourceCache` URL params) keep `string` — brand stops at cache boundary; `MeshComponent.nodePath` and FileSystemDirectoryHandle strings are NOT branded (different domains)
- 2026-05-05 [janitor]: cleared 21 stale items (9 archive-cache md, 1 scheduled-tasks lock, 12 twilight-* preview HTMLs); split `### 回應節奏` 55 lines from CLAUDE.md → `.claude/style-guide.md` (CLAUDE.md ~3175→~2300 tokens); clarified MEMORY.md path is auto-memory at `~/.claude/projects/<project>/memory/`
- 2026-05-07 [arch]: Schema v1 spec amendments merged (PR #815) — `nodeType` enum replaces `components` bag, `position/rotation/scale` full names, `version` literal field, prefab strict reference (no expand into parent SceneFile.nodes[]), `HexColor` at JSON boundary + `number` at runtime, env block enters SceneFile, `userData: {}` reserved; commander principle "嚴格取代鬆散"; implementation across ~30 files follows in 3 phases (foundation / switchover / invariants)
- 2026-05-07 [arch]: Schema v1 switchover merged (PR #819) — flat nodeType across 41 files (1135+/2128-), prefab pure reference (InstantiatePrefabCommand no longer expands into SceneFile.nodes), env block in SceneFile + AutoSave subscribes envChanged, asset URLs stay `assets://` at JSON boundary (runtime blob URLs held in `SceneSync._resolvedBlobUrls` map, never mutate `node.asset`), `userData = {}` invariant enforced, `v0_to_v1` round-trip guard via `typeof node.nodeType === 'string'` passthrough; PrefabInstanceWatcher kept but dormant (Phase 3 evaluate removal); minor visual flash on first sceneReplaced rebuild (clear order tradeoff, not data-loss) tracked for Phase 3
- 2026-05-07 [arch]: Schema v1 Phase 3 invariants merged (PR #823 schema + PR #822 reference & broken-ref UI) — Zod schema validate `ErythosSceneV1` covering 10 條結構/欄位/大小 invariant, `SceneInvariantError` with path+reason, `UnsupportedVersionError` 對 future-version files, deserialize 順序 `checkRawVersion → v0_to_v1 → validateScene`, AutoSave 寫前再驗;PrefabGraph cycle detect (self-ref 接線完整;N-hop 受 PrefabFormat 不編碼 nested prefab refs 限制,defer 至 #824), `SceneSync._brokenRefIds` 標記 + `bridge.brokenRefIds()` signal + `brokenRefsChanged` event 在 rebuild 後發 + `new Set(...)` 包裝避開 SolidJS `===` shallow check, TreeNode 紅標 (`.row.brokenRef .nodeName` 後代選擇器), `BrokenRefsBadge` toolbar chip;follow-up:#824 (N-hop production 接線需 PrefabFormat 擴充)、Phase 2 follow-up 仍待 (PrefabInstanceWatcher 死代碼移除評估、首次 sceneReplaced 視覺 flash);PR #823 漏 commit lockfile,main 補 `752740a` 緊急 sync zod 4.3.6 (npm 環境 SSL leaf-cert 問題暫關 strict-ssl 才裝得進去)
- 2026-05-07 [arch]: Schema v1 Phase 3 follow-up wrap-up — PR #826 (#824) wires N-hop cycle detection through `PrefabRegistry` 6 mutation paths (loadFromURL / set / attach refetch / evict / evictByPath / clear) using `components['prefab'] = { asset }` convention to encode nested prefab refs in `PrefabNode`; AD chose `setEdges(url, [])` over `invalidate(url)` on evict because invalidate only clears deps cache leaving stale outbound edges (false-negative after re-add) — QC validated;PR #825 cleanup removes `PrefabInstanceWatcher` + test (-496 lines, dormant since v1 prefab children stay in Three.js scene only, not SceneDocument; design doc `prefab-inline-edit.md` retained for future reinstate) and adds `.claude/`-only filter to `.githooks/pre-commit` (skips package.json bump on meta-only commits);visual flash 自查 — PR #822 已 land `Editor.loadScene` 末尾的 `sceneSync.rebuild() + brokenRefsChanged` emit,final state 正確,完全消除首次空 rebuild 留作 P3 perfection
- 2026-05-07 [arch]: panel anatomy 五件套 land (PR #828, refs #827) — `<Panel> / <PanelHeader> / <PanelToolbar> / <PanelContent> / <PanelFooter>` + `<PanelEditorSwitcher>` 解耦 useArea/useEditorsRegistry 從 PanelHeader 拆出;4 panel migration(scene-tree/properties/project/viewport)以 `<Panel>` 為 root,`PanelContent scrollable` prop 控 overflow,viewport `scrollable={false}` + 內保留 `.canvas` 包 RenderSettingsPanel / ErrorDialog / dropOverlay;Panel/PanelToolbar/PanelContent 用 `splitProps + class concat` 讓 caller 可注入 panel-specific class(QC 抓的 class-prop-dropped bug 修正);AD 自決 viewport `--bg-app → --bg-panel` 違反 spec 視覺 1:1,QC 退回,改 viewport.module.css `.viewportRoot { background: var(--bg-app); }` + `<Panel class={styles.viewportRoot}>` override 預設 bg-panel;接續 P1 panel root ESLint rule(initiatives.md C 區)
- 2026-05-07 [scripts]: panel root contract 機械化 (PR #829) — `scripts/check-panel-root.mjs` 從 `src/panels/<dir>/index.ts` 的 `component:` field 反查 dockview-registered panel(透過 EditorDef 註冊才算,自動排 RenderSettingsPanel 等浮動 overlay,不需黑名單),對每個 panel TSX 驗 `return ( <Panel ...` pattern,lookahead `(?=[\s/>\n])` 避免誤匹配 `<PanelHeader` 等同前綴元件;併入 `npm run check` chain(`check:command-pattern && check:panel-root`);順手同步 `scripts/new-panel.mjs` template 為五件套(`<Panel>` + `<PanelHeader>` + `<PanelContent>`)+ CSS 拿掉 `.panel` root rule(由 `components/Panel.module.css` 接管),確保 future scaffold 立刻 PASS
- 2026-05-07 [arch]: cloud sync spec v0 land (PR #831) — Step 3 of 「編輯器的核心功能設計」13 輪對談收尾;`docs/sync-protocol.md` 釘 scene blob sync(單一 Linode VPS 全包 / Postgres BYTEA / optimistic concurrency via HTTP `If-Match` etag RFC 7232 / 409 Conflict 跳對話框 keep-local 或 use-cloud + `.erythos.bak` 安全網,diff-merge 進 v2)、帳管 7 題已決(anonymous-usable、share/sync 才登入、email-only、multi-device sync only、GDPR day 1、Figma fork model、anonymous→registered prompt 提示)、認證(Better Auth + GitHub OAuth-only + Postgres `sessions` 表 + HttpOnly session cookie,Lucia 2024 已 deprecated 不選,magic link + Resend 列 v0.1 後加題);`docs/asset-sync-protocol.md` 釘 binary asset sync(Linode Object Storage S3-compatible、content-addressed sha256 in URL `assets://<sha256>/<filename>`、HEAD-before-POST dedup、`Cache-Control: immutable` 永久 cache、free 500MB / pro 50GB quota、自動 GC v0 不做),cross-ref 既有 broken-ref UI (PR #822);Phase A-C 完成前不開 Linode 機器避免 ops 雜事卡客戶端進度;Step 4 假 migration 練肌肉早被 v0→v1 真升版替代達成,4 步全 closed
- 2026-05-11 [deps]: dependabot esbuild GHSA-67mh-4wv8-2f99 (CVSS 5.3 moderate) 接受不修 — 來源 `drizzle-kit → @esbuild-kit/esm-loader → @esbuild-kit/core-utils → esbuild@0.18.20`,`@esbuild-kit/core-utils` 上游 deprecated,nested overrides 嘗試後 npm 不 re-resolve transitive(`npm install --force` 或刪 lock 風險過大);影響面僅 dev server 暴露(non-CORS protection),production 不暴露;remediation 等 drizzle-kit 上游切 tsx-only 或我們自動化 deploy 改用 tsx CLI;優先 issue / drizzle-team upstream PR 是未來路徑
- 2026-05-12 [ops]: DB backup land prod — Linode Object Storage 開 `erythos-backups` bucket on Tokyo 3 (region slug `jp-tyo-1`), `S3_ENDPOINT` 必須 **region-only** `https://jp-tyo-1.linodeobjects.com`(不是 dashboard 顯示的 `https://<bucket>.<region>.linodeobjects.com` virtual-hosted),否則 aws CLI 雙重 bucket 解析失敗;AWS CLI v2.34+ / boto3 默認 checksum 行為對 Linode 不相容(response 缺 header → `argument of type 'NoneType' is not a container or iterable`),`.env` 補 `AWS_REQUEST_CHECKSUM_CALCULATION=when_required` + `AWS_RESPONSE_CHECKSUM_VALIDATION=when_required` + `AWS_REGION=jp-tyo-1` 三條即可;backup.sh 走 cron 0 3 * * * daily,prune 7 天 rolling;deploy 流程目前只 copy `server/deploy/install.md` + `Caddyfile.example` + `erythos-server.service`,**`backup.sh` 未自動 deploy** 要 prod 端 raw 從 github main 抓(follow-up:CI #952 deploy.yml 帶 `server/deploy/` 全目錄)
- 2026-05-12 [ops]: SSH brute-force fail2ban 對家用浮動 IP 友善處理 — 浮動 IP 不能加 `ignoreip` whitelist(IP 過期會 protect 陌生人),正解是統一所有客戶端走 SSH key 而非 password;LISH(Linode dashboard web console)是 fail2ban 卡死時唯一 fallback,在 LISH 內可 `fail2ban-client unban <ip>` 解封;偵錯期間建議 `jail.d/sshd-loose.conf` 暫設 `maxretry=20` + `bantime=600`,避免每次 SSH 嘗試就 ban 自己
- 2026-05-12 [ops]: SSH publickey-only deploy 需 `sshd_config` 設 `PermitRootLogin prohibit-password`(別名 `without-password`),**不是** `no`;default `no` 會讓 publickey 認證即使 key 正確也失敗 — verbose log 印 `Server accepts key: ... agent` 後直接 fallback 試下個 key 並回 `Permission denied (publickey)`,容易誤判為 `publickey-hostbound-v00` Windows agent bug 或 algorithm mismatch,實際是 sshd 在認證決策前 hard-deny root;LISH 內 `sshd -T 2>&1 \| grep -i permitrootlogin` 一行確認 active 值
- 2026-05-12 [ops]: 現行 `.github/workflows/deploy.yml` **只 deploy client** (`dist/` → `/opt/erythos/client/releases/`),完全不碰 server 端;`/opt/erythos/server/deploy/` 內目前 3 個檔(`install.md` + `Caddyfile.example` + `erythos-server.service`)是 prod 初次 manual setup 時上傳,後續新增的 `backup.sh` / `restore.md` / 任何 ops script 都要從 github main raw 抓 — follow-up #980:deploy.yml 加 checkout + sync `server/deploy/` 整目錄到 `/opt/erythos/server/deploy/` step
- 2026-05-12 [phase-f]: F-1 Asset Sync 全 land prod(epic #957)— server F-1a/b/c (#960/#961/#962) S3 client + assets table + `/api/assets` HEAD/POST/GET + 11 tests;client SDK F-1d-1 (#963) `HttpAssetClient` + `AssetResolver` cache;wire F-1d-2a (#964) Editor 構造子 optional `assetClient?` + App.tsx 注入;dragon F-1d-2b (#973) `uploadSceneBinaries` + `HttpSyncEngine` hook(rebuild SceneDocument via `getAllNodes() + addNode()` bypass `deserialize()` v1_to_v2 migration 的 `rewriteAssetScheme` 反轉問題,MVP workaround);quota UI F-1d-2c (#972) `/auth/me` server endpoint + UserMenu 配額顯示;ops:`erythos-assets` bucket on Tokyo 3 同 backup 區、access key 共用既有(`server/src/storage/s3.ts` 吃 `AWS_ACCESS_KEY_ID` 單把,日後權限分離再 refactor);prod smoke `GET /api/assets/<zero-hash>` 回 404(預期);follow-up 全 closed:✅ #974 v1_to_v2 hash-form guard (PR #978) / ✅ #975 spec scene asset URL 欄位 § (PR #976)
- 2026-05-12 [core]: v1_to_v2 `rewriteAssetScheme` hash-form pass-through guard(PR #978 closes #974)— JSDoc 原寫「leaves cloud-form unchanged」但 code 沒實作 guard,所有 `assets://` URL 一律 rewrite 為 `project://`,導致 v2-native scene 走 `deserialize()` 時合法 `assets://<hash>/<filename>` URL 被錯誤 downgrade;加 `/^assets:\/\/[0-9a-f]{64}\//` 正規式 guard,SHA-256 hex 形式 pass-through,只 legacy v1 path-form 才 rewrite;4 tests 補(path-form rewrite / hash-form pass-through / round-trip / future-safe);F-1d-2b PR #973 的 `getAllNodes() + addNode()` workaround **後續以 PR #982 清理**(改回 `serialize() → patch raw JSON → deserialize()` round-trip);regex 小寫 only,若未來 cloud 端用大寫 hex 需加 `i` flag
- 2026-05-12 [core]: `uploadSceneBinaries.ts` round-trip cleanup(PR #982 closes #979)— 取代 F-1d-2b dragon path 的 `getAllNodes() + addNode()` workaround,改用 `serialize() → patch raw JSON (env.hdri + nodes[i].asset) → deserialize()` 路徑;依賴 PR #978 的 hash-form regex guard 保證 `deserialize()` 不會反向 downgrade `assets://<hash>` URL;順手修 idempotent test fixture(`'assets://abc123hash/texture.png'` short hash → `'a'.repeat(64)` 對齊 64-hex 規則);783/783 tests pass;F-1 dragon 路徑現乾淨
- 2026-05-12 [scripts]: deploy.yml 加 `server/deploy/` 整目錄 sync step(PR #981 closes #980)— `deploy` job 加 `actions/checkout@v6` step + 新 step `scp -r server/deploy/. → /opt/erythos/server/deploy/` + `chmod +x *.sh`
- 2026-05-12 [ops]: CI/CD prod 全通 — 3 個 secret 設好(`SSH_PRIVATE_KEY` 用新生的 ed25519 deploy key `erythos_deploy`,公鑰加到 prod `/root/.ssh/authorized_keys`;`VPS_HOST=erythos.eoswolf.com`;`VPS_USER=root`),run `25711495857` workflow_dispatch 觸發,build 30s + deploy 24s = 54s 全綠;`/opt/erythos/server/deploy/` 上 sync 後有 5 檔(`Caddyfile.example` / `backup.sh` / `erythos-server.service` / `install.md` / `restore.md`),`backup.sh` + `restore.md` 是新增到 prod 的;之後 push main 自動 deploy client 跟 server/deploy/ ops scripts
- 2026-05-12 [ops]: Observability metrics prod activated — `/opt/erythos/server/.env` 補 `METRICS_USER=metrics` + `METRICS_PASS=<openssl rand -hex 16>`,`systemctl restart erythos-server` 後 `/api/metrics` 無 auth 回 401 / 帶 basic auth 回 200 + JSON `{uptime_seconds,counters{req_total,auth_*,scene_*,asset_*},timestamp}`;prod hardening 5/5 達成(DB backup / F-1 assets bucket / CI/CD secrets / server/deploy sync / observability)
- 2026-05-12 [ops]: deploy.yml server build/sync 仍是盲點 — PR #981 只加 `server/deploy/` ops scripts 同步,**server TS source 跟 `server/dist/` 都不自動 deploy**;清 #953 時發現 prod HEAD 卡 `8ee3963` 落後 main 67 commits / `server/dist/index.js` mtime `May 11 09:48`、未含 `metricsRoutes` mount,手動 `git pull + npm install + npm run -w server build + db:migrate (0003 magic-link + 0004 assets) + systemctl restart` 拉齊;follow-up #983 追 F-2-extend(`npm ci` 取代 `npm install` 防 lock drift / migration gated by `server/migrations/**` diff / `/health` smoke gate)
- 2026-05-12 [client]: F-5 D1 magic-link client UI land prod(PR #993)— `AuthClient.requestMagicLink(email)` + `SignInDialog` modal(GitHub OAuth button primary + email input form + 「Check your inbox」sent state + HTML5 email validity 預驗 + focus trap + ESC/backdrop close,跟 DeleteAccountDialog 同 pattern)+ `Toolbar` Sign in click 改開 dialog(原 direct OAuth navigate 改成 modal pick GitHub or email)+ `AuthErrorBanner` extend `expired`/`used`/`invalid`/`rate_limited` 4 個 magic-link verify error codes(reuse #920 OAuth banner pattern)+ `bridge` `requestMagicLink` 接 + `App.tsx` `authRequestMagicLink: (email) => authClient.requestMagicLink(email)` wire;17 new tests(6 AuthClient + 11 SignInDialog),802/802 client tests pass + build pass。advisor 第二輪 review 採 3 建議:GitHub button 用 `onOpenOAuth` callback(test 不用 stub `window.location`)/ HTML5 `validity.valid` 防 malformed input 走 server round-trip / `AuthErrorBanner` namespace shared 注釋
- 2026-05-12 [ops]: prod `.env` 6 條 land + `magic_link_tokens` 表 owner 修(closes #992)— append `ALLOWED_ORIGIN=https://erythos.eoswolf.com`(原 unset default `localhost:5173`,所有 state-changing POST 全 403,F-1 asset upload + 未來 magic-link / scene save 都受影響)/ `S3_ENDPOINT` / `S3_BUCKET` / `S3_ASSETS_BUCKET` / `S3_REGION=jp-tyo-1`(`storage/s3.ts:27` 讀 `S3_REGION ?? 'ap-south-1'`,prod 沒設 default 是 Singapore region vs Tokyo bucket,F-1 asset endpoint 配置一直壞)/ `MAGIC_LINK_BASE_URL=https://erythos.eoswolf.com`;手動跑 0003 SQL with `sudo -u postgres psql` 留下 owner=postgres 後遺症,`ALTER TABLE magic_link_tokens OWNER TO erythos` 修;memory `feedback_manual_sql_user_ownership.md` 補規則防再踩
- 2026-05-12 [server]: F-5 C2 magic-link endpoint + rate limit + stub email land prod(PR #989)— `requestMagicLink` / `verifyMagicLink` 邏輯展開(SELECT+INSERT+UPDATE drizzle pattern),`POST /api/auth/magic-link/request` + `GET /verify`,in-memory sliding-window rate limiter(per-email 60s silently absorbed for anti-enum + per-IP request 10/h exposed 429 + per-IP verify 20/min);**advisor 抓 IP spoofing 雷**:`clientIP()` 改讀 `X-Forwarded-For` 最後一個 entry(Caddy append 真 IP 到尾端,client-supplied entries 在前 — 取 [0] 會被 spoof bypass per-IP rate limit);**寄信暫 stub**(`console.log(plaintext)` to stdout,C3 SDK 接通後拿掉)— 加 inline SECURITY comment;12 tests + 88/88 pass。**prod smoke 揭露 ops gap**:`/opt/erythos/server/.env` 漏 `ALLOWED_ORIGIN`(default `localhost:5173`,所有 state-changing POST 全 403)+ `S3_*` 也缺,follow-up #992。Follow-up 也開 #990 concurrent verify race(atomic UPDATE WHERE used_at IS NULL RETURNING)+ #991 Caddyfile defense-in-depth header_up XFF strip
- 2026-05-12 [server]: F-5 C1 `users.github_id` NOT NULL → nullable land prod(PR #987 + hotfix `4f19cf5`)— migration 0005 + schema.ts type 改 + `AuthUser` interface + 2 route `Variables` type augmentation 都改 `number | null`。**踩雷**:本機只跑 root `npm run build`(只 vite client),沒跑 `npm run -w server build`,CI deploy-server step 跑 server tsc 才抓到 `auth.ts:120 TS2322`,main broken,直 commit hotfix。同時實際撞 advisor 預警(#988)build-fail-before-health-gate gap → \`set -e\` exit 後 rollback 沒 fire,prod 卡在「git@TARGET / new build broken / DB unmigrated / service@PREV」一致狀態。Hotfix run 後 \`MIG_DIFF\` 空(PREV=c199683 TARGET=4f19cf5 都含 0005),migrate 跳過 → 手 ssh 跑 db:migrate apply 0005 + 手跑 0003 SQL CREATE TABLE 補(drizzle 認為 0003 已 apply 但 prod 從未實際有 magic_link_tokens 表,屬 drizzle ops historical drift,#986 跟進)。memory `feedback_server_build_before_server_pr.md` 補規則防再踩
- 2026-05-12 [scripts]: deploy.yml `deploy-server` job land(PR #984 + #985 closes #983)— SSH 到 VPS 跑 `git reset --hard <github.sha>` + `npm ci`(嚴格 lockfile)+ `npm run -w server build` + `db:migrate` gated by `server/migrations/**` diff + `systemctl restart erythos-server` + `/health` smoke gate(`"status":"ok"` 拒收 degraded)+ 失敗自動 rollback 到 `PREV_SHA`;**concurrency `group=deploy, cancel-in-progress=false`** serial queue 避免 race-reset prod 到舊 SHA;**server first, client second**(`deploy needs: deploy-server`)避免 client 升完但 server API 舊版的 mismatch window;`TARGET_SHA` 用 positional arg `bash -s "$TARGET_SHA"` 而非 env-in-cmd-string(quote-safe);**rollback 不 re-run `db:migrate`** — drizzle 是 forward-only;**out-of-scope**:destructive migrations(DROP / ALTER TYPE)CI rollback 會 schema-code 不匹配,PR 須 manual deploy(inline comment 標記);兩段 rollout 驗 happy path:commit 1 `if: workflow_dispatch only` 手動 trigger run `25713123618` 全 pass(npm ci 7s / no migration diff / health 22ms),commit 2 拿掉 gate + 鏈順序 + `.gitignore client/`(prod-only ops dir 雜訊);first push-trigger run `25713242397` build 25s + deploy-server 19s + deploy 26s = 70s 全綠
- 2026-05-11 [phase-e]: Phase E client-server wiring + GDPR land — 切 HttpSyncEngine + currentUser signal (#918) / Toolbar avatar chip (#921) / auth_error banner (#920) / viewer-mode AuthError + Fork sign-in (#928) / viewer mode on 200+no-local (#930) / `/api` prefix all routes (#926) / client baseUrl `/api` (#925) / Caddy SPA fallback + atomic deploy (#927);GDPR § 41 day-1:server `GET /api/me/export` + `DELETE /api/me` + `saved_by` SET NULL migration (#934) / client User menu Export+Delete + DeleteAccountDialog (#936) / `clearSessionCookie` 屬性鏡像 hotfix (8ee3963 — 缺 HttpOnly/Secure/SameSite=Lax 會留下殘餘 cookie);test latent type errors NodeUUID 等 brand 修補 (#933);`docs/sync-protocol.md` GDPR 章節同步;follow-up #935 drizzle journal 補登已修(idx 1 = 0001_hash_session_tokens,下次 prod migrate 會 re-apply truncate sessions = 一次性 force re-login)

- 2026-05-09 [server]: D3 OAuth 走 self-rolled minimal(Option C,PR #893),Better Auth 留 dep 給未來 magic link / email-pwd — Better Auth 需 account+verification 表會偏離 D2 schema § 資料模型;NIT-1 redundant non-timing-safe equality + NIT-2 unvalidated `?redirect=` open-redirect inline 修;NIT-3 session token plaintext at rest 留 #894 follow-up
- 2026-05-09 [server]: D2 Postgres schema 用 Drizzle ORM + drizzle-kit(PR #892),4 表(users / sessions / scenes / scene_versions)逐欄對齊 spec § 資料模型;bytea 用 customType(drizzle v0.45 沒原生),self-ref FK 用 `(): AnyPgColumn =>` 解 TS7022/7024 環引推斷;pgcrypto NIT 留 D5 驗 PG ≥ 14(#890),scene_versions.saved_by ON DELETE NO ACTION 留 D4 帳號刪除 endpoint 評估(#889)
- 2026-05-09 [server]: D1 server scaffold land(PR #886)— monorepo + npm workspaces,server/ 子目錄,Hono 0.50+ + @hono/node-server 適配 Node runtime,Better Auth + pg 列入 dep skeleton 但實作仍 stub;9 endpoints 全 401/501;.env 進 gitignore 只 commit .env.example

- 2026-05-09 [server]: D4 5 REST endpoints 真實 + If-Match etag land(PR #896)— bytea wire = inline JSON object(Buffer.from(JSON.stringify) write / JSON.parse(toString) read)對齊 client HttpSyncEngine;PUT 區分 428 missing / 412 malformed / 409 mismatch;GET auth-optional(spec 允許 anonymous 看 public),其餘 4 個強 auth;owner check 全 404 不洩露存在性(PUT 原 403 inline 修 → 404 對齊 PATCH/fork);31 tests PASS;follow-up #897 (413 1MB payload limit) / #898 (client 端不處理 428 latent)
- 2026-05-09 [server]: D5 sync server 上線 `https://erythos.eoswolf.com`(Linode Tokyo 4GB Ubuntu 24.04)— host 已有 Caddy + Docker(backlog + gridbot)co-locate,改 Caddy site block `reverse_proxy 127.0.0.1:3000` + auto-HTTPS Let's Encrypt 取代 D5a 規劃的 nginx + certbot --standalone;Postgres 16 + Node 22 + systemd `erythos-server`(`ProtectSystem=strict` + `RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX` hardening)同機共用;Cloudflare DNS-only(grey cloud,orange proxy 會壞 OAuth cookie domain + 擋 ACME HTTP-01);`DB_PASS` 用 `openssl rand -hex 24` 不用 base64 24(後者含 `/+=` 需 URL encode);psql 用 `-P pager=off -c` 一行一條而非 heredoc(PowerShell ssh client paste 會在每行加 leading 兩格,讓 `EOF` terminator 不被認得);OAuth 端到端煙測通(`/auth/me` 200 + user UUID/github_id);D5a 寫死的 nginx + certbot 文件 (`server/deploy/nginx.conf` + `install.md` Phase 4/8/10) 需 follow-up PR 改 Caddy 路線

- 2026-05-10 [server]: VPS NOPASSWD sudoers (`/etc/sudoers.d/erythos`) 取代密碼 sudo — single-admin、ssh key 為第一道,密碼二道在自動化路徑只是摩擦;LISH 救援後設定,後續工具命令路徑 `sudo -n` 一律過
- 2026-05-10 [server]: VPS deploy ssh client 標準改 git-bash;PowerShell `Get-Content | ssh` 送 CRLF 給 bash 噴 `\r: command not found`,救援用 `[IO.File]::ReadAllText('script').Replace("\`r\`n","\`n") | ssh user@host 'bash -s'`
- 2026-05-10 [server]: Caddy security headers 抽 `(common_security_headers)` snippet 在 host `/etc/caddy/Caddyfile` 頂宣告,3 site (gridbot/backlog/erythos) 各自 `import` 共用(PR #904 改 example;host 同步動)— inventory 發現只有 erythos site 帶 STS/X-Frame/Referrer-Policy
- 2026-05-10 [server]: VPS 多住戶模型維持(erythos+gridbot host node;backlog docker;Postgres 一台多 db;Caddy 多 site)— inventory audit 結論架構已乾淨,搬 erythos 進 docker 半搬 ROI 負,systemd 命名 `erythos-server` vs `gridbot` 不一致無功能影響不動
- 2026-05-10 [server]: `/auth/me` wire shape 對齊 client `AuthClient`(PR #903)— server 改回 `{id, github_login, email, avatar_url}`,internal `AuthUser` widen 不外洩 `github_id`/`handle`/`storage_used`;Phase E 啟動前 must-fix,audit 抓
- 2026-05-10 [server]: Hono 1MB body limit middleware 在 4 write endpoint apply (PR #905, refs #897)— Caddy 已 outer fence,Hono 內 fence 守 dev 直連 localhost:3000;header-only 快路徑,streaming(無 Content-Length)pass through 不擋
- 2026-05-10 [docs]: spec § 認證實作 / D5 milestone 文字對齊現實(PR 待開)— Better Auth dep skeleton retained / OAuth 走 self-rolled HMAC (D3 Option C) / nginx → Caddy auto-HTTPS;實作早 land,spec 文字回頭改貼近落地

- 2026-05-11 [server]: Caddy `(common_security_headers)` snippet 在 basicauth site 必須含 `handle_errors { header { ... } }` 第二份配置才會套到 4xx/5xx response — Caddy `header` directive(即使 `defer`)對 basicauth short-circuit 401 不 apply,因為 basicauth 用自己 short-circuit pipeline 不走 directive chain;snippet 結構:外層 `header { defer ... }` 對 200,內層 `handle_errors { header { ... } }` 對 4xx/5xx;驗證 3 site (erythos 404 / gridbot 401 basicauth / backlog 200) 全套 4 個 security header
- 2026-05-11 [server]: backlog (`backlog.eoswolf.com`) 走 Cloudflare orange proxy(`Server: cloudflare` + `cf-cache-status` + `CF-RAY` + NEL),不是 grey cloud — 跟 erythos `Cloudflare DNS-only(grey cloud)` trust model 不同;snippet 套到的 header 仍透過 CF 傳到 client(STS / X-Frame / Referrer-Policy 都 OK),但 `-Server` 抹掉 origin Caddy 的 Server,CF outbound 又補回 `Server: cloudflare`;屬 backlog 部署選擇,不影響 erythos OAuth (erythos 必須 grey cloud 否則 cookie domain 壞)
- 2026-05-11 [server]: npm audit 5 moderate(全同一條:`drizzle-kit → @esbuild-kit/esm-loader → @esbuild-kit/core-utils → esbuild <=0.24.2`,GHSA-67mh-4wv8-2f99)標 known-not-applicable — vulnerability 是 esbuild dev server CSRF-like,我們不跑 esbuild dev server(client 用 vite,server 是 tsc + node-server);drizzle-kit 是 devDependency,production runtime 不 import,attack surface 為 0;`npm audit fix --force` 會 breaking downgrade drizzle-kit 0.45 → 0.18.1 壞 schema/migrate,不接受;等 drizzle-kit upstream pin 新 esbuild 再 `npm update -w server drizzle-kit` 即可
- 2026-05-11 [server]: #894 session token SHA-256 hash + #898 client `PreconditionRequiredError` 上 prod(commit `af3d8e2`);migration `0001_hash_session_tokens.sql` TRUNCATE sessions(現有 OAuth user 全要重 sign-in,Phase D 階段測試 user 一兩個影響小);prod `/health` 200 + 全套 security header 套到 OK

- 2026-05-11 [phase-e]: E1-E7 全完工 + prod deploy(epic #913)— E1+E2 (#918) `HttpSyncEngine` 取代 `LocalSyncEngine` + `currentUser` signal `Accessor<User | null | undefined>`(undefined = unresolved 防 reload flicker)+ bridge `currentUser` / `signOut` / `getOAuthStartUrl`;E3 (#921) Toolbar `UserMenu` 三態 Portal dropdown(mockup Option A:Toolbar 右側 avatar chip);E4 (#920) `AuthErrorBanner` + onMount detect `?auth_error=<code>` + `history.replaceState`(三 code: missing_code / invalid_state / oauth_failed);E5 (#919) `HttpSyncEngine` 收緊 visibility/forked_from type + 移過時 fallback(#910 確認虛假缺口 — D4 #896 早補,client TODO comment 誤導 audit);E7 path split — server 全 routes mount `/api/*` (#926) + client `defaultBaseUrl()` 加 `/api` (#925) + Caddyfile `handle /api/*` reverse_proxy + SPA `file_server + try_files index.html` fallback + install.md Phase 12 atomic symlink flip 部署 (#927) — 解 `GET /scenes/:id` server API 跟 client SPA viewer route 撞牆;e2e prod sign-in pass(指揮家瀏覽器手動 smoke 全流程);follow-up open:#917 viewer-mode AuthError catch 細化 / #911 匿名 Fork 引導登入 / #912 GDPR 導出+刪帳 / #889 scene_versions.saved_by ON DELETE policy
- 2026-05-11 [agents]: `Agent` tool `isolation: "worktree"` 在 Windows 上仍可能 leak git ops 到主 repo — Sonnet AD-A 跑 Phase E E7-1 (#922) 時雖然 isolation worktree dir 建立(`.claude/worktrees/agent-<id>/`),實際 `git checkout -b feat/e7-1-...` + commit + push + PR (#926) 全在 `C:/z/erythos` 主 repo 跑;污染主 repo branch(被切走)+ working tree 部分 mod 被 AD 的 git ops reset(Caddyfile mod 蒸發,install.md mod 倖存);dispatch prompt 須強制 AD 第一步跑 `pwd && git rev-parse --show-toplevel` 確認 cwd 在 worktree 內,git 操作前 `cd "$(git rev-parse --show-toplevel)"` 對齊;memory `feedback_ad_isolation_worktree_windows_leak.md` 記

- 2026-05-12 [backlog]: 6-issue 並行 batch land(#988 deploy.yml migrate trap PR #994 / #991 Caddyfile XFF strip PR #995 / #986 drizzle meta 0003+0004 snapshot backfill PR #996 / #937 tech debt v0_to_v1 single cast PR #997 / #783 BlobURL brand SceneSync 3 行 PR #998 / #990 magic-link verify atomic UPDATE PR #999)— 4 server/infra worktree 真並行 + 1 client 並行 dispatch,1 hour wall clock 全 land + prod deploy + close;#937 #783 #785 audit drift 揭露,實際 diff 比 issue spec 小 1 個數量級;`refs #N` 規則被多 AD 違規寫 `Closes #N`(影響:auto-close 加速,實質不可逆)

- 2026-05-12 [ops]: prod `/opt/erythos/.git/refs/remotes/origin/{fix,dependabot}` + `logs/refs/remotes/origin/{fix,dependabot}` 為 root-owned 導致 deploy.yml 的 `sudo -u erythos git fetch` 失敗(`Permission denied` cannot lock ref `fix/*`)— root cause 為早期 manual ssh prod debug 用 sudo / root 跑過 git fetch,留下 root-owned children;修法 `chown -R erythos:erythos` + rm stale `worktree-agent-*` refs/logs;`git fetch --prune` 該移除遠端不存在的 stale refs 但 ref ownership 不對時 prune 也跑不動;deploy.yml rerun 過 build+deploy-server+deploy 三 jobs 全 success,prod a4aa199 健康(`/health` ok,db up,9ms);防再犯應加 deploy.yml 一步「`sudo chown -R erythos:erythos .git`」defensive

- 2026-05-12 [ops]: deploy.yml 加 defensive `sudo chown -R erythos:erythos .git`(PR #1001)— 每次 deploy 在 `cd /opt/erythos` 後立刻跑,確保所有 .git 子檔案 erythos-owned,後續 `sudo -u erythos git fetch` 不再踩 ref permission lock 雷;chown -R idempotent + .git 規模小成本可忽略 + 覆蓋整個 .git(refs / logs / packs / config / hooks)未來任何 root-owned 漂移(eg. manual debug 用 sudo)都被 deploy 自動修正;首次 deploy run 25730XXX 驗證

- 2026-05-13 [phase-f]: F-5 Phase C+D 完整 prod live + e2e 通過 — C1 schema relax `users.github_id` nullable (PR #987) / C2 endpoint + rate limit + stub (PR #989) / C3 Resend SDK + email template + env-gated (PR #1002) / D1 client SignInDialog + AuthClient.requestMagicLink + AuthErrorBanner extend (PR #993),atomic UPDATE verify race fix (PR #999),Resend ap-northeast-1 region + DNS SPF/DKIM/DMARC 已 verify、API key rotated 2 次(初次 leak in conversation transcript 已 revoke),真實 e2e wolfuardian@gmail.com 信收到 Gmail 主收件匣(domain alignment 達標不進 spam)+ template 正確(主旨 / button / 15min 提示 / disclaimer / plain text fallback)+ 點 link 302 / + Set-Cookie session 成功;15 分鐘 TTL 觸發過期(指揮家第一次點是 20 分後),atomic burn → 第二次點顯式 `used` 行為對 spec;TTL 暫留 15 分鐘 default(replay protection 設計)

- 2026-05-13 [ops]: prod env 補 `RESEND_API_KEY` + `MAGIC_LINK_FROM_EMAIL=noreply@erythos.eoswolf.com`(預設 default 在 code 內,顯式設較易維護)— Origin guard 擋 curl 沒帶 `Origin` header 的 POST 回 403(預期,#992 修過的 ALLOWED_ORIGIN 仍生效),需 `-H "Origin: https://erythos.eoswolf.com"` 才通;Caddy 反代 + atomic verify 路徑健康(02:35 verify 302 duration 10ms),deploy 鏈乾淨
- 2026-05-14 [v0.2-release]: L1 cross-device sync 9-case 手測全 PASS — release criteria #1 通過;v0.2 code 端 release blocker 全 land(T2/T3/T5/T6 bug fix + UUID validation sweep + cross-tab echo storm fix),剩 onboarding design(#1030 #942 release criteria #4 #7)+ release ops(tag + CHANGELOG)
- 2026-05-14 [arch]: cloud project lifecycle in-process integration test land(`src/__tests__/cloud-project-lifecycle.integration.test.ts`)— 整條 createCloudProject → loadScene → addNode → autosave → push → conflict resolve 用 fake-fetch in-process server mock cover,8 case;比 prod 手測迴圈快幾百倍(每輪 5 秒 vs 5 分鐘 + deploy quota)且永久進 CI guard;以後 e2e 只 reserve「真正未知」如 mobile UX / 真實 network latency;memory `feedback_integration_test_before_e2e` 記
- 2026-05-14 [bug]: T3 prod 409 race root cause = CloudAutoSave.flushNow 無 mutex,debounce 多 fire 時 push #2 帶 stale baseVersion;修法 `lastFlush` promise chain serialize 同 flushNow 多 caller(PR #1028)
- 2026-05-14 [bug]: Editor.loadScene line 301 `if (this.syncEngine)` create 路徑對 cloud project 也跑,mint 第二個 server scene 並蓋 syncSceneId — 加 `syncSceneId === null` guard;cloud path 在 App.tsx 預設 set syncSceneId 後再 loadScene,正確 skip create(PR #1028)
- 2026-05-14 [bug]: cross-tab reload echo storm — A push 廣播 → B reload deserialize → sceneReplaced event → AutoSave schedule push → 反向廣播 → A reload → 無限 ping-pong,in-flight user mutation 被覆蓋成「自動回溯」;`AutoSaveHandle.suppress(on)` refcounted,App.tsx cross-tab handler wrap deserialize 防 echo(PR #1034)
- 2026-05-14 [arch]: ERROR CODE pattern 落地 seed(#1025)— server response body `{error: <human>, code: "E#### ERR_…"}`,E1001 ERR_USER_ID_FORMAT(users)+ E1002 ERR_SCENE_ID_FORMAT(scenes 全 :id routes 套 middleware);新 user-facing throwable 該循同 shape,舊 throwable 漸進 migrate 不一次 retrofit;`server/src/middleware/validate-uuid.ts` `requireUuidParam` 抽 factory 防 non-UUID input 落 Postgres uuid column 直接 500(PRs #1031 #1032)
- 2026-05-14 [scene]: empty scene blob on cloud create — App.tsx POST /api/scenes 原送 `body:{}` 致 load 噴 `SceneInvariantError: version undefined`;LocalProjectManager.createScene 也寫 `{version:1,nodes:[]}` 舊 schema;統一抽 `createEmptyScene()` factory at `core/scene/io/types.ts`(current v3 shape `{version:3, upAxis:'Y', env:{...}, nodes:[]}`),兩 callsite import — schema 升級 v4 時改一處(PR #1024)
- 2026-05-14 [scene]: `uploadSceneBinaries` walker 漏 skip `project://primitives/*` — cloud save 加 cube(`node.asset='project://primitives/box'`)時呼叫 LocalProjectManager.readFile 但 cloud mode 沒 open project → throw "No project open";Editor.ts:260 已 special-case primitives 路徑,push 路徑補同 skip;長期 schema 改用 dedicated `primitives://` scheme(#1027)(PR #1026)
- 2026-05-14 [v0.2-release]: release criteria #7 onboarding cold-start 手測桌機 9-case 全 PASS(A 組 cold start + 免登入 local / B 組 cloud warm-resume / C 組 sign-in → cloud create → lifecycle)— #1030「Welcome 無登入入口」最小修法 land(PR #1035,footer Sign in 按鈕 + SignInDialog mount,完整 onboarding 版面留 criteria #4);v0.2 code 端 criteria #1/2/3/5/6/7 全綠,剩 #4 onboarding product input(指揮家 design 決策);手測發現 #1036 cloud ProjectChip 空白(bridge.projectName 讀未 open 的 LocalProjectManager singleton 而非 cloud manager,非致命不擋)
- 2026-05-14 [core]: #1036 fix — cloud ProjectChip 空白根因 `HttpSyncEngine.fetch()` 把 server payload 的 `name` 丟掉、`bridge.projectName` signal 又只讀未 open 的 LocalProjectManager singleton;修法 fetch() return 補回 name → CloudProjectManager 加 `get name()` getter(cloud-only API,不上 ProjectManager interface,同 sceneId/currentVersion 模式)→ `openCloudProject` 穿進 `createEditorBridge` 新 optional `deps.projectName`;涵蓋 create / 開清單 / cold-start resume 三路徑(都在 build bridge 前跑 loadScene);offline cache-hit 路徑無 name 屬既定降級(PR #1037,905 tests + regression assertion)
- 2026-05-14 [app]: v0.2 criteria #4 onboarding 版面 — 指揮家逐條 align 5 決策,做 3(未登入右欄加 cloud placeholder 卡開 SignInDialog / 已登入空 cloud 狀態變可點 button 開 modal 預選 Cloud / footer signed-in 死字清掉 + dead `.footerSub` CSS 刪)、defer 2(feature tour + sample gallery → v0.2.x;Quick Start tiles 不動);新 `NewProjectModal.initialKind` prop 走 `createEffect(show())` 每次開啟套用;兩 sign-in 入口(footer + 新卡)改 onClick 設 `signInTriggerRef` 防 render-time ref 互蓋(PR #1038);順手修 spec § Onboarding「每 scene 5MB」自相矛盾值 → 1MB 對齊 sync-protocol body limit

- 2026-05-13 [ops]: F-5 reaper cron 啟用 prod(PR #1005 + ssh ops)— `server/deploy/reaper-magic-link-tokens.sh` 走 erythos user crontab `0 4 * * *` daily,SQL `DELETE FROM magic_link_tokens WHERE expires_at < NOW() - INTERVAL '30 days'`,保留 30 天供 ops grep-audit;首次 dry-run 0 row(row 都 < 1 天舊);log 在 `/var/log/erythos-reaper.log`;backup cron 在 root crontab(install.md Phase 13 慣例),reaper 改 erythos crontab(least privilege + 對齊 reaper.sh chown 設定)— two user 不一致但功能 OK,future reset 看 doc 即可

- 2026-05-13 [release]: v0.1.0 first public release tagged `2783665` — local-first 3D editor positioning,F-1~F-6 六 phase 全 land(F-1 asset sync / F-2 CI-CD / F-3 single-device hardening / F-4 observability / F-5 magic link + Resend / F-6 perf 0.62→0.94 LCP 6.5s→2.6s);CHANGELOG.md 誠實標 Known limitations(F-3 phantom code #1003/#999/#1004 在 v0.1 local-first 架構無 UX hook,留作 v0.2 cloud project 地基 + 多 tab in-memory gap #1006);multi-device 共編 / cloud project 排 v0.2 first-class milestone(AA 戰略報告 `.claude/scratch/cloud-project-strategy-2026-05-13.md`,scope:L1 同 user 跨 device + 半個 L2 share URL 唯讀,L3 real-time co-edit 排 v0.3);marketing 改口徑 "Local-first 3D editor — cloud sync coming in v0.2" 避免 "sign in for backup" 誤導(client 未 pull = dead drop)

- 2026-05-13 [v0.2-scope]: v0.2 cloud project scope 拍板 — 採 AA 戰略報告推薦(L1 同 user 跨 device + 半個 L2 share URL 唯讀;L3 real-time/ACL 排 v0.3);onboarding/quota/pricing 採 § 7.2 default working hypothesis(scene 10 / 5MB / 500MB asset / empty placeholder / 登入 only / early access free unlimited);Phase 切 6 個 G1-G6(ProjectManager interface / CloudProjectManager impl / UI / startup flow / share URL / offline UX);release criteria 加碼版 7 條(原 5 + share URL e2e 手測 + onboarding flow user testing);spec draft 在 `.claude/scratch/cloud-project-spec-draft.md`,後續整理成正式 `docs/cloud-project-spec.md` 進 git 後開 v0.2 epic + 6 sub-phase issue

- 2026-05-13 [v0.2-readiness]: v0.2 release criteria #3 marketing 文案 + #5 Resend cost / session TTL 落地 — #3 marketing(Welcome footer `v0.2 — Local + cloud 3D editor` / `Sign in to sync across devices` + NewProjectModal Local/Cloud 二選一 + Toolbar cloud sync indicator + SignInDialog GitHub primary + email secondary)在 G3 PR #1019 內已隨手對齊 spec § Marketing 改口徑,無額外改動;#5 session cookie TTL 從 30 天拉長到 90 天(`server/src/auth.ts` `SESSION_TTL_MS`)— Resend cost 緩解 #1 路徑,repeat visitor 跨 90 天內回訪不重新走 magic link;其餘緩解策略(OAuth primary path / magic link 不自動 retry / anonymous viewer 不過 sign-in)在 F-5 D1 + G5 #1014 + G3 #1010 內已落地;v0.2 code 端 release criteria #1/2/3/5/6 全綠,剩 #4(onboarding product input)+ #7(user testing)為指揮家手測 / design 階段

- 2026-05-15 [v0.3-scope]: v0.2 criteria #4 onboarding working hypothesis 被指揮家修正 → v0.3 workstream — quota scene 10→3 / asset 500MB→150MB,placeholder empty→server 端 user 建立時自動 provision 1 個 Demo scene(cube + 一盞燈,算配額、使用者可自刪),免登入試用不變;版本拍板 v0.3.0;切 3 chunk 依序 land:C 刪 cloud project(DELETE /scenes/:id owner-only + ProjectChip dropdown delete UI,PR #1040)/ A quota enforcement(POST /scenes 擋 3 上限 → E1003 + asset 150MB const,PR #1043)/ B Demo provision(`shared/onboarding/demo-scene.json` 單一來源:server import 來 provision、client test import 同檔驗;OAuth + magic-link 兩 auth path 包 db.transaction 保 user-insert + demo-insert atomic,PR #1045);spec § Onboarding working hypothesis → v0.3 拍板(be474b7)

- 2026-05-15 [app]: v0.3 prod 手測 bugfix(PR #1046)— (1) cloud ProjectChip dropdown 看不到 Delete project 按鈕:`bridge.projectType()` 讀 `editor.projectManager.type`,但 cloud 的 `editor.projectManager` 是未開的 LocalProjectManager singleton(`Editor.ts:57` readonly,cloud 不開 LocalProjectManager)→ 永遠回 'local',delete gate `=== 'cloud'` 永遠關;修法鏡像 #1036 projectName — `EditorBridgeDeps` 加 `projectType?`、cloud 路徑顯式傳 'cloud'(同 gate 連帶修好 Toolbar Cloud sync indicator,latent 自 G3,未實測待手測確認)(2) 撞 quota 時 error 只顯示 `403`:`App.tsx` 建 cloud project 失敗只丟 HTTP status,改解析 response body `{error, code}` 以 `${human} (${code})` 並列(client 端 E100x 顯示首次定型);assets.ts + HttpAssetClient 同 class 缺口(無 E-code、client 吞 body)→ #1047,relates #1025
