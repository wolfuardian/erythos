# chore/remove-scene-ops-overlay

## 需求
移除 ViewportPanel 中不再需要的 `sceneOpsOverlay` CSS class 及其對應的 JSX 用法。

## Scope
- `src/panels/viewport/` — ViewportPanel CSS module（刪除 `.sceneOpsOverlay` 規則）及 JSX（移除對應 DOM 元素或 className 引用）

## 風險 / Trade-off
- 若 overlay 內仍有掛載的 UI 元素，移除後那些元素會消失，需確認是預期行為

## 不做的事
- 不動其他 ViewportPanel 樣式或結構
- 不重構 overlay 為其他形式

---
Generated by /work-start, 2026-05-06
