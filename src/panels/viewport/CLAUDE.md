# viewport 模組 CLAUDE.md

## 範圍限制

- 只修改 `src/panels/viewport/ViewportPanel.tsx` 和新增 `src/viewport/dropPosition.ts`
- 不動其他模組
- 不改 UI 行為，純重構

---

## 當前任務

<!-- 由主腦在指派任務時填寫 -->

---

## 待修項

（QC FAIL 時由 AH 填入）
