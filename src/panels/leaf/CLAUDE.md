# Leaf Panel 模組

## 範圍限制
只能修改 src/panels/leaf/ 底下的檔案。
不得修改 src/core/、src/viewport/、src/components/、src/app/、src/panels/scene-tree/、src/panels/properties/、src/panels/viewport/。

## 當前任務
<!-- 由主腦在指派任務時填寫 -->

## 慣例
- 透過 bridge.leafAssets() 讀取 leaf 資產
- 用 SolidJS createSignal 管理面板內部狀態
- 樣式用 inline style + CSS 變數 var(--bg-*)

## 待修項（由主腦根據 QC issue 填寫）
## 上報區（供主腦 review）
