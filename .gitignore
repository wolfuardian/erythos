node_modules
dist
stats.html
*.tsbuildinfo
*.local
.DS_Store
.version-bumped
samples
settings.local.json
.claude/user/
.claude/previews/
.claude/worktrees/
.claude/codebase-spec/
.claude/session/
.claude/scratch/*
!.claude/scratch/.gitkeep
.claude/scheduled_tasks.lock
projects/
.playwright-mcp/
.superpowers/
.idea/

server/.env

# Prod ops dir (`/opt/erythos/client/releases/...`) — gitignored so prod
# `git status` doesn't noise on each deploy-server run.
client/

# Client env (Vite) — .env.example is tracked, actual secrets are not
.env
.env.*.local
